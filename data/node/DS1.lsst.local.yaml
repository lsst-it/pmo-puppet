---
classes:
  - profile::default
  - profile::monitoring_graylog
  - profile::backup_directories
  - profile::backup_aws
  - profile::nagios_cfg
profile::backup_directories::drive_letter: d
profile::backup_aws::drive_letter: d
profile::backup_aws::bucket_path: ds1server/documents
    # Require - Taksk Name
    # String $sch_task_name = 'Backups AWS'
#profile::backup_aws::sch_task_name: 'Backups AWS'
    # Require - Script and Location to run
    # String $sch_script = '$drive_letter:/backups/scripts/backup_aws_sync.bat
#profile::backup_aws::sch_script: '/backups/scripts/backup_aws_sync.bat'
    # Require - Period (frequency)
    # String $sch_period = 'daily' # Need to lookup valid values for ms task scheduler
profile::backup_aws::sch_period: 'daily'
    # Require - Start Time
    # String $sch_starttime = '11:28'
profile::backup_aws::sch_starttime: '11:45'
    # Required values shall be declared in node yaml
    # To Do Needs to have creds for AWS (protected info)
#profile::nagios_cfg::node_name: ds1
#profile::nagios_cfg::alias_name: somethingnospace 
#profile::nagios_cfg::node_ip: 140.252.32.104
profile::nagios_cfg::host_groups: web,vm-win-guest
profile::nagios_cfg::drive_letter: d

# Create a tuple with check_command, service_description, servicegroups
#[$check_command, $service_description, $servicegroups]= ["check_nt!SERVICESTATE!-d SHOWALL -l MpsSvc","windows FFFWall", "services-firewall"]


